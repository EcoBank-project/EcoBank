<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecobank.app.sns.mapper.FileMapper">

	<!-- 등록 -->
	<insert id="insertFileInfo" parameterType="FileVO">
		<selectKey keyProperty="fileNo" 
					resultType="Integer"
					order="BEFORE">
			SELECT NVL(MAX(file_no),1)+1
			FROM file
		</selectKey>
		INSERT INTO file 
		(
			 file_no	
			 ,file_name
			 ,file_path
			 ,file_type
			 ,file_code
			 ,file_code_no )
		VALUES
		(
			#{fileNo}
			,#{fileName}
			,#{filePath}
			,#{fileType}
			,#{fileCode}
			,#{fileCodeNo}
			)
	</insert>
	

</mapper>